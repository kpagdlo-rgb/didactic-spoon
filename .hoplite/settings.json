{
  "scripts": {
      "setup": "python3 -m venv .venv && .venv/bin/pip install -r requirements-preview.txt && cd apps/meder && bun install --frozen-lockfile",
      "run": "cd apps/meder && bun run dev -- --port ${PORT:-3000}"
  },
  "ports": { "default": 3000 }
}
