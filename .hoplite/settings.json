{
  "scripts": {
    "setup": "python3 -m venv .venv && .venv/bin/pip install -r requirements-preview.txt",
    "run": ".venv/bin/python scripts/docs_preview.py"
  },
  "ports": { "default": 3000 }
}
